using System.Collections;
using System.Collections.Generic;
using System.Security.Cryptography;
using UnityEngine;

public class Player_Controller_29 : MonoBehaviour
{
    float speed = 10.0f;
    float maxlimit = 4.5f;
    Rigidbody RB;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Declare and Init variables to reference to User Interaction inouts
        float verticalInput = Input.GetAxis("Vertical");
        float horizontalInputt = Input.GetAxis("Horizontal");

        //Move plyer (Gameobject) according to user interactions
        







    }
}
